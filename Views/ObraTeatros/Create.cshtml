@model OWCodigo5.Models.ObraTeatro

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ObraTeatro</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="IdObra" class="control-label"></label>
                <select asp-for="IdObra" class ="form-control" asp-items="ViewBag.IdObra"></select>
            </div>
            <div class="form-group">
                <label asp-for="IdTeatro" class="control-label"></label>
                <select asp-for="IdTeatro" class ="form-control" asp-items="ViewBag.IdTeatro"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
