@model IEnumerable<OWCodigo5.Models.ObraTeatro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Obra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teatro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Obra.Titulo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teatro.IdTeatro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdObraTeatro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdObraTeatro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdObraTeatro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
